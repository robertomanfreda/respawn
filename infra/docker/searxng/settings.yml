use_default_settings: true

general:
  instance_name: "Respawn SearXNG"

search:
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false
  public_instance: false
  secret_key: "respawn-local-dev-secret"
  method: "GET"
